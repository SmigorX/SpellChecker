#rename this file to env.py
secret = "your discord app secret"
allowed_user_ids = ["int list of user IDs the bot will respond to"]
allowed_channel_ids = ["int list of channels bot is allowed to respond in"]
